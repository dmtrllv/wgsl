import { DiagnosticsContext } from "@wgsl/core";
import { Token } from "@wgsl/lexer";

export const parseTokens = (_path: string, _tokens: Token[], _ctx: DiagnosticsContext) => {
	console.log("parse tokens")
};